name: SecurityCheck

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Sourcing pdt tooling.sh
      run: |
        . ./pdt help
    - name: Running security report
      run: |
        pdt full-security-check
